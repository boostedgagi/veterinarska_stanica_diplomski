version: '3'
services:
  mysql:
    image: percona:8.0
    restart: always
    ports:
      - '3326:3306' # 'local_port:port_in_container'
    environment:
      - MYSQL_ROOT_PASSWORD=MYSQL_RANDOM_ROOT_PASSWORD
#      - DB_HOST=127.0.0.1
      - MYSQL_DATABASE=vet_shop
      - MYSQL_USER=admin
      - MYSQL_PASSWORD=password
#  mailer:
#    image: schickling/mailcatcher
#    ports: [ 1025, 1080 ]
  phpmyadmin:
    image: phpmyadmin
    container_name: phpmyadmin_vet_shop
    links:
      - mysql
    environment:
      PMA_HOST: eu-cdbr-west-03.cleardb.net
      PMA_ARBITRARY: 1
      MYSQL_USERNAME: 'b56af87347d7c7'
      MYSQL_ROOT_PASSWORD: 'f57784a7'
    restart: always
    ports:
      - "8082:80"
